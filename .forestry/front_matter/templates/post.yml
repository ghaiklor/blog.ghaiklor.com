---
label: Post
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
    min: 
  label: Title
  description: The name of a blog post
- name: excerpt
  type: text
  config:
    required: true
    min: 80
  label: Excerpt
  description: Take (a short extract) from a post
- name: categories
  type: tag_list
  default: []
  label: Categories
  description: Provide a category that is suitable for the post
- name: tags
  type: tag_list
  default: []
  label: Tags
  description: Provide a list of tags that are appliable to the post
- name: header
  type: field_group
  config: {}
  fields:
  - name: teaser
    type: file
    config:
      maxSize: 64
    label: Teaser
    description: Teaser image used in the “Related Posts”
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
    description: A large full-width header image
  - name: caption
    type: text
    config:
      required: true
    label: Caption
    description: A caption or attribution for the image, e.g. "Photo Credit"
  label: Header
  description: A large full-width header image, its teaser and other header related
    configuration
pages:
- _posts/how-does-node.js-work.md
