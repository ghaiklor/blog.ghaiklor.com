---
label: Post
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: The name of a blog post
- name: excerpt
  type: text
  config:
    required: true
    min: 80
  label: Excerpt
  description: Take (a short extract) from a post
- name: header
  type: field_group
  config: {}
  fields:
  - name: teaser
    type: file
    config:
      maxSize: 64
    label: Teaser
    description: Teaser image used in the “Related Posts”
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: caption
    type: text
    config:
      required: true
    label: Caption
  label: Header
